(*Set boundary variables*)
(*Max height Mario can jump*)
let max_jump = 3;;
(*Max distance Mario can jump*)
let max_dist = 3;;
(*Height of ground based on number of grids*)
let ground_height = 2;;

(*Canvas is 480 by 320 (w*h) -> 30 by 20 blocks
* Let the first generaed map just be of size 5 by 5 blocks *)



(*Procedurally generates a new map of default size
* Return a list (maybe array) holding all the objects generated on the canvas
* Each object should hold a (record?) containing the coordinates and type
* TEMP
*)
let generate () = failwith "TODO"

